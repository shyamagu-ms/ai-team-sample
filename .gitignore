.playwright-cli
# Local PoC artifacts (including the legacy directory)
/poc/
/poc-*/

# Local secrets and browser authentication state
.env
.env.*
!.env.example
auth.json
storage-state-*.json
*.auth-state.json
